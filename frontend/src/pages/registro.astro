---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Crear cuenta — PixelCrit" description="Únete a PixelCrit y empieza a compartir tus opiniones sobre videojuegos.">
  <div class="min-h-[80vh] flex items-center justify-center px-4 py-16">
    <div class="w-full max-w-md">

      <div class="text-center mb-8">
        <h1 class="font-bungee text-3xl text-text-primary uppercase mb-2">Crear cuenta</h1>
        <p class="text-text-muted">Únete a la comunidad de PixelCrit</p>
      </div>

      <div class="bg-dark-secondary rounded-2xl border border-white/5 p-8">

        <div id="error-general" class="hidden mb-6 bg-red-500/10 border border-red-500/20 text-red-400 rounded-lg px-4 py-3 text-sm"></div>

        <form id="form-registro" novalidate>

          <!-- Username -->
          <div class="mb-5">
            <label for="username" class="block text-sm font-medium text-text-secondary mb-2">
              Nombre de usuario
            </label>
            <input
              type="text"
              id="username"
              name="username"
              autocomplete="username"
              placeholder="gamer_pro_99"
              class="w-full bg-dark-primary border border-dark-tertiary text-text-primary placeholder-text-muted rounded-lg px-4 py-3 focus:outline-none focus:border-primary transition"
            />
            <p id="error-username" class="hidden mt-1.5 text-xs text-red-400"></p>
            <p class="mt-1 text-xs text-text-muted">3-20 caracteres, solo letras, números y guiones bajos.</p>
          </div>

          <!-- Email -->
          <div class="mb-5">
            <label for="email" class="block text-sm font-medium text-text-secondary mb-2">
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              autocomplete="email"
              placeholder="tu@email.com"
              class="w-full bg-dark-primary border border-dark-tertiary text-text-primary placeholder-text-muted rounded-lg px-4 py-3 focus:outline-none focus:border-primary transition"
            />
            <p id="error-email" class="hidden mt-1.5 text-xs text-red-400"></p>
          </div>

          <!-- Password -->
          <div class="mb-5">
            <label for="password" class="block text-sm font-medium text-text-secondary mb-2">
              Contraseña
            </label>
            <div class="relative">
              <input
                type="password"
                id="password"
                name="password"
                autocomplete="new-password"
                placeholder="••••••••"
                class="w-full bg-dark-primary border border-dark-tertiary text-text-primary placeholder-text-muted rounded-lg px-4 py-3 pr-12 focus:outline-none focus:border-primary transition"
              />
              <button
                type="button"
                id="toggle-password"
                class="absolute right-3 top-1/2 -translate-y-1/2 text-text-muted hover:text-text-secondary transition"
                aria-label="Mostrar contraseña"
              >
                <svg id="eye-show" xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                </svg>
                <svg id="eye-hide" xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.88 9.88l-3.29-3.29m7.532 7.532l3.29 3.29M3 3l3.59 3.59m0 0A9.953 9.953 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.025 10.025 0 01-4.132 5.411m0 0L21 21"/>
                </svg>
              </button>
            </div>
            <p id="error-password" class="hidden mt-1.5 text-xs text-red-400"></p>
            <p class="mt-1 text-xs text-text-muted">Mínimo 8 caracteres.</p>
          </div>

          <!-- Confirm Password -->
          <div class="mb-6">
            <label for="confirm-password" class="block text-sm font-medium text-text-secondary mb-2">
              Confirmar contraseña
            </label>
            <input
              type="password"
              id="confirm-password"
              name="confirm-password"
              autocomplete="new-password"
              placeholder="••••••••"
              class="w-full bg-dark-primary border border-dark-tertiary text-text-primary placeholder-text-muted rounded-lg px-4 py-3 focus:outline-none focus:border-primary transition"
            />
            <p id="error-confirm" class="hidden mt-1.5 text-xs text-red-400"></p>
          </div>

          <button
            type="submit"
            id="btn-submit"
            class="w-full bg-primary hover:bg-primary/90 disabled:opacity-60 disabled:cursor-not-allowed text-white font-semibold rounded-lg px-6 py-3 transition flex items-center justify-center gap-2"
          >
            <span id="btn-text">Crear cuenta</span>
            <svg id="btn-spinner" class="hidden w-5 h-5 animate-spin" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 12 0 12 12H4z"></path>
            </svg>
          </button>
        </form>

        <p class="text-center text-text-muted text-sm mt-6">
          ¿Ya tienes cuenta?
          <a href="/login" class="text-primary hover:text-primary/80 font-medium transition ml-1">Inicia sesión</a>
        </p>
      </div>
    </div>
  </div>
</Layout>

<script>
  const API_URL = '/api';

  if (localStorage.getItem('pixelcrit_token')) {
    window.location.href = '/';
  }

  const form = document.getElementById('form-registro') as HTMLFormElement | null;
  const btnSubmit = document.getElementById('btn-submit') as HTMLButtonElement | null;
  const btnText = document.getElementById('btn-text') as HTMLElement | null;
  const btnSpinner = document.getElementById('btn-spinner') as HTMLElement | null;
  const errorGeneral = document.getElementById('error-general') as HTMLElement | null;

  const toggleBtn = document.getElementById('toggle-password') as HTMLButtonElement | null;
  const passwordInput = document.getElementById('password') as HTMLInputElement | null;
  const eyeShow = document.getElementById('eye-show') as HTMLElement | null;
  const eyeHide = document.getElementById('eye-hide') as HTMLElement | null;

  toggleBtn?.addEventListener('click', () => {
    if (!passwordInput) return;
    const isPassword = passwordInput.type === 'password';
    passwordInput.type = isPassword ? 'text' : 'password';
    eyeShow?.classList.toggle('hidden', !isPassword);
    eyeHide?.classList.toggle('hidden', isPassword);
    toggleBtn.setAttribute('aria-label', isPassword ? 'Ocultar contraseña' : 'Mostrar contraseña');
  });

  // Regex patterns
  const RE_EMAIL = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  const RE_USERNAME = /^[a-zA-Z0-9_]{3,20}$/;

  function showFieldError(fieldId: string, msg: string) {
    const el = document.getElementById(fieldId);
    if (!el) return;
    el.textContent = msg;
    el.classList.remove('hidden');
  }

  function clearErrors() {
    ['error-username', 'error-email', 'error-password', 'error-confirm'].forEach(id => {
      const el = document.getElementById(id);
      if (el) { el.textContent = ''; el.classList.add('hidden'); }
    });
    if (errorGeneral) {
      errorGeneral.textContent = '';
      errorGeneral.classList.add('hidden');
    }
  }

  function setLoading(loading: boolean) {
    if (btnSubmit) btnSubmit.disabled = loading;
    if (btnText) btnText.textContent = loading ? 'Creando cuenta...' : 'Crear cuenta';
    btnSpinner?.classList.toggle('hidden', !loading);
  }

  function validate(username: string, email: string, password: string, confirm: string) {
    let ok = true;

    if (!username.trim()) {
      showFieldError('error-username', 'El nombre de usuario es obligatorio.');
      ok = false;
    } else if (!RE_USERNAME.test(username)) {
      showFieldError('error-username', 'Solo letras, números y guiones bajos. Entre 3 y 20 caracteres.');
      ok = false;
    }

    if (!email.trim()) {
      showFieldError('error-email', 'El email es obligatorio.');
      ok = false;
    } else if (!RE_EMAIL.test(email)) {
      showFieldError('error-email', 'Introduce un email válido.');
      ok = false;
    }

    if (!password) {
      showFieldError('error-password', 'La contraseña es obligatoria.');
      ok = false;
    } else if (password.length < 8) {
      showFieldError('error-password', 'La contraseña debe tener al menos 8 caracteres.');
      ok = false;
    }

    if (!confirm) {
      showFieldError('error-confirm', 'Confirma tu contraseña.');
      ok = false;
    } else if (password !== confirm) {
      showFieldError('error-confirm', 'Las contraseñas no coinciden.');
      ok = false;
    }

    return ok;
  }

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    clearErrors();

    const username = (document.getElementById('username') as HTMLInputElement).value.trim();
    const email = (document.getElementById('email') as HTMLInputElement).value.trim();
    const password = (document.getElementById('password') as HTMLInputElement).value;
    const confirm = (document.getElementById('confirm-password') as HTMLInputElement).value;

    if (!validate(username, email, password, confirm)) return;

    setLoading(true);
    try {
      const res = await fetch(`${API_URL}/auth/local/register`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ username, email, password }),
      });

      const data = await res.json();

      if (!res.ok) {
        const msg = data?.error?.message ?? 'No se pudo crear la cuenta. Inténtalo de nuevo.';
        if (errorGeneral) {
          errorGeneral.textContent = msg;
          errorGeneral.classList.remove('hidden');
        }
        return;
      }

      const { jwt, user } = data;
      localStorage.setItem('pixelcrit_token', jwt);
      localStorage.setItem('pixelcrit_user', JSON.stringify({
        id: user.id,
        username: user.username,
        email: user.email,
      }));
      window.location.href = '/';
    } catch (err) {
      if (errorGeneral) {
        errorGeneral.textContent = 'Error de conexión. Comprueba tu red e inténtalo de nuevo.';
        errorGeneral.classList.remove('hidden');
      }
    } finally {
      setLoading(false);
    }
  });
</script>
